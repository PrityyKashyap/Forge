/**
 * The MemTable: the sorted, in-memory, mutable staging structure that
 * absorbs writes between flushes. See ARCHITECTURE.md &sect;3.1 and
 * DESIGN.md &sect;11 (Phase 3 storage-engine contract).
 */
package com.forge.storage.memtable;
